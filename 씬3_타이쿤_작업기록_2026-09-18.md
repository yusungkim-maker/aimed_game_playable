# 씬 3 타이쿤 — 작업 기록 2026-09-18

프로젝트: `np_playable_set` / 씬 `assets/scenes/tycoon.scene`
서술형 요약은 바탕화면 `씬3_타이쿤_작업정리_2026-09-18.md`, 게임 흐름 명세는 `np_playable_set/docs/tycoon_확정명세.md`.

---

## 0. 오늘 끝난 것

| # | 항목 | 성격 |
|---|---|---|
| 1 | 나무 2종 · 건물별 분기 (11/11/11, 미배정 0) | 기능 |
| 2 | 적재 소켓이 건물 건설 전까지 비활성 | 기능 |
| 3 | 일꾼 소환 이펙트 배선 | 연출 |
| 4 | 일꾼 수 2D UI "n/10" | 기능 |
| 5 | 철 일꾼 경로 점(`Point0…`) | 기능 |
| 6 | 창고 없으면 일꾼이 짐을 지고 선다 | 규칙 변경 |
| 7 | ground_dirt · 울타리 등장 확대 연출 | 연출 |
| 8 | 나무가 화면 좌/우로 눕는다 | 연출 |
| 9 | `CoinStack` 레인별 회전 | 기능 |
| 10 | `ScatterExclude` 회전 반영 | 기능 |
| 11 | `ScatterField.씬에 굳히기` | 기능 |
| 12 | 막힘 박스를 플레이 중에 보기 | 디버그 |
| 13 | 재배치 지연이 "아예 안 깔리는" 버그였던 것 수정 | 버그 |
| 14 | 사고 2건(툴팁 줄바꿈 / onLoad 비활성화)과 복구 | 사고 |

---

## 1. 나무 분포

| | `TreeField` | `TreeFieldDirt` |
|---|---|---|
| 프리팹 | `T_Tree` (다시 자람) | `T_Tree_Fixed` (한 번 베면 끝) |
| `영역 안쪽에만 깔기` | 꺼짐 (흙바닥 **밖**) | 켜짐 (흙바닥 **안**) |
| 개수(상한) / 시작·최대 반지름 / 간격 | 250 / 4·25 / 1.2 | 200 / 1.5·12 / 1.2 |
| 사라짐 분기 | 없음 | A=Trigger1 WorkerLodge · B=Trigger2 Warehouse · C=Trigger4 Blacksmith |

`분기를 균등하게 나누기`를 **켠다**. 끄면 "가장 가까운 건물"로 배정되는데, 건물이 한쪽에 몰려 있어 실측에서 **WorkerLodge 몫이 0그루**가 나왔다(그 건물을 지어도 아무것도 안 사라진다). 켠 상태 실측: **33그루 = 11 / 11 / 11, 미배정 0** → 대장간까지 지으면 흙바닥에 나무 0그루.

### 첫 배치는 `start()`에서 한다

`제외를 씬 전체에서 수집`은 다른 노드의 `ScatterExclude`가 자기 `onEnable`에서 채우는 static 목록을 읽는다. 엔진은 노드를 **계층 순서대로** 활성화하므로 `onLoad` 시점에는 그 목록이 비어 있을 수 있다. 그러면 안쪽 필드는 "깔 곳이 없다"고 판정해 **한 그루도 안 깐다**(에러 없음). `start()`는 씬의 모든 `onLoad`/`onEnable` 뒤라 안전하다.

### `ScatterExclude`가 회전을 반영한다 (신규)

사각형 표현을 `[minX, minZ, maxX, maxZ]` **4칸 → `[중심x, 중심z, 반경x, 반경z, cos, sin]` 6칸**으로 바꿨다. 옛 형태로는 회전을 표현할 수 없어 노드를 돌려도 비워지는 자리가 그대로였다.

- 각도는 노드의 **월드 Y 회전**(부모 포함). 바닥에 눕는 판이라 X·Z 기울기는 무시한다.
- `중심 오프셋 X/Z`도 사각형이 돌아간 방향을 따라 같이 돈다.
- 판정은 **점을 사각형의 로컬 좌표로** 옮겨서 본다: `lx = dx*cos - dz*sin`, `lz = dx*sin + dz*cos`.
  월드→로컬은 회전의 역이라 부호가 반대다. **45도 비대칭 각도로 양방향 검산 완료.**
- 회전 0이면 cos=1, sin=0이라 예전과 수치가 완전히 같다. **씬 1·2는 `ScatterExclude` 사용 0건.**

`BuildSpots` 노드가 **Y −45도**를 갖고 있어 `ground_main`·`ground_sub`가 그 회전을 물려받는다. 두 **영역** 사각형이 흙바닥 마름모와 같은 방향으로 눕는다. `ground_main` 노드는 흙바닥 중심(월드 0,0)에 있으므로 반경 두 개만 맞추면 된다 — 계산상 **7.69 × 5.07**(화면 확인은 아직).

### 건물 5곳의 제외가 죽어 있다 (미해결)

`ScatterExclude`의 **컴포넌트 체크박스**(`_enabled`)가 꺼지면 `onEnable`이 안 불려 **레지스트리에 등록조차 안 된다.** `제외 끄기`(프로퍼티)와 다른 것이다.

실측: 8개 중 **5개가 꺼짐** → `ScatterExclude.all = 3`.
꺼진 것: `SwordShop` `WorkerLodge` `Warehouse` `Blacksmith` `ironMine`
→ **이 건물 5곳은 지금 나무를 전혀 밀어내지 못한다.** 켤지 판단 대기.

### 손으로 배치·삭제 — `씬에 굳히기`

체크하면 지금 깔린 대로 **DontSave 없이** 다시 깔아 씬에 저장되게 한다. 굳힌 뒤에는 `update()`가 자동 재생성을 멈추고 `_clear()`도 **DontSave 붙은 것만** 지운다 — 손으로 옮긴 개체는 어떤 경로로도 안 지워진다.

**성능**: 런타임 비용은 생성 방식과 무관하고 **개수만이 정한다.** 차이는 빌드 용량뿐(gzip 후 그루당 약 0.2KB). 진짜 한계는 **드로우콜** — 카드 머티리얼 `TextureCardOffset.mtl`에 `USE_INSTANCING`이 **없다**(실측).

### 재배치 지연이 곧 "안 깔리는" 버그였다

`dt`를 모아 타이머를 깎는 디바운스라 **update가 최소 두 번 돌아야** 반영됐다. 에디터는 화면에 변화가 있을 때만 update를 돌리는 경우가 있어 **틱이 한 번 오고 끝난다.** 그러면 타이머가 줄지 못해 영영 다시 깔리지 않는다(시드를 바꿔도 배치가 그대로였다).
→ **벽시계 스로틀**로 교체. 값이 바뀌면 그 틱에서 즉시 다시 깔고, 그 뒤 `재배치 지연` 동안 무시한다.

---

## 2. 일꾼 · 적재 소켓

### 원인 하나가 증상 셋을 냈다 — 스폰 순서

`CoinCourierManager._spawnCourier`가 **값을 넣기 전에** `parent.addChild(node)`를 했다. `addChild`는 `onLoad`를 그 자리에서 부르므로 `CoinCourier.onLoad`가 프리팹 기본값을 읽었다:

- `absorbTriggerId`가 None으로 보임 → **창고 전에도 자원을 내려놓음**
- `mineNode`가 null로 보임 → 광산 일꾼 몸이 안 사라짐
- `wagonNodeName`이 빈 값 → 달구지가 안 나옴

→ `CoinCourier`에서 **인스펙터 값을 읽는 초기화를 전부 `start()`로** 옮겼다.
**매니저가 프리팹을 instantiate 해 값을 주입하는 컴포넌트는 `onLoad`에서 그 값을 읽으면 안 된다.**

### 창고가 없으면 일꾼이 들고 있는다 (규칙 변경)

`CourierRoute.absorbTriggerId`는 프로퍼티 이름만 두고 **의미를 바꿨다**: `자원 흡수 시작` → **`내려놓기 시작`**. 못 하는 것이 가져가기가 아니라 내려놓기다.

- `canAbsorb`는 **항상 true** (바닥에 안 떨어뜨린다)
- 새 상태 `Holding` — 가득 차면 그 자리에 짐을 진 채 선다(달구지도 보인다)
- 가득 차기 전에 나무가 넘어가면 **다음 나무로 간다**
- 출발 판정을 `_deliverOrHold()` **한 곳**에 모았다 — 벌목·채굴·무더기 세 경로에 흩어져 있어 하나만 빠뜨려도 없는 창고로 걸어가 영영 못 내려놓는다

### 적재 소켓 게이팅

`CoinGroundStack.등장 트리거`(신규, 기본 None = 기존 동작).

| 소켓 | 트리거 |
|---|---|
| WarehouseStack_Wood | 2 |
| WarehouseStack_Iron | 3 |
| ShopStack_Sword · ShopStack_Coin | 4 |
| ForgeIn_Wood · ForgeIn_Iron · ForgeOut_Sword | 4 |

**`node.active`를 끄지 않는다**(4절 사고 참조). `start()`에서 **렌더러만** 끄고 `_hidden` 플래그로 `update`·`reserveSlot`·`takeOne`을 막아 받지도 돌려주지도 않게 한다.

### 소환 이펙트 · 일꾼 수 UI · 경로

- **소환 이펙트**: 기능은 원래 있었고 배선만 비어 있었다. 규칙 9대로 씬 3 사본을 만들어 꽂았다 — `tycoon/models/efffect2.glb`(@b4cf5) + `tycoon/materials/GlowFx.mtl` + `tycoon/effects/GlowFx.effect` + `tycoon/textures/vfx1.png`(mtl과 glb.meta의 uuid를 사본으로 리맵). 크기 0.7. `asset_operations copy`로 복사하면 **서브에셋 id가 유지**된다.
- **일꾼 수 UI**: 신규 `WorkerCounterUI` + `CoinCourierManager.countFor(triggerId)`. Canvas에 `WorkerCounter_Wood`(노선 1)/`WorkerCounter_Iron`(노선 3), `WorldUIAnchor`로 **고용소 위에 따라다닌다**(높이 1.6). 등장 트리거 1/3. **아직 글자만 있다** — 배경·아이콘 없음. `AtlasNumber`를 못 쓴 이유는 `number.png`에 **슬래시 글자가 없어서**다.
- **철 일꾼 경로**: `CourierRoute.경로 노드`(신규). 자식 `Point0` `Point1` …을 0부터 끊김 없이 읽어 그 순서로 지나고 **돌아올 때는 역순**. 비우면 직선. 씬에 `IronWorkerPath`(Point0 `(0.5,0,-3.8)` / Point1 `(-3.2,0,-2.6)`) — **HQ를 남쪽으로 우회시킨 계산값이고 눈으로 맞춘 적 없다.**

---

## 3. 연출 · 기타

- **등장 확대**: `BuildingTrigger.등장 확대 시간(초)`(신규, 기본 0) — ground_main·sub 0.45. `FenceBuilder.등장 확대 시간` 0.3 + `조각별 등장 간격` 0.04. 원래 크기는 **`onLoad`에서 미리** 잡는다(연출 도중 읽으면 0을 원래 크기로 착각한다).
- **나무 쓰러짐**: `Choppable.쓰러지는 축 (월드)`(신규, 기본 (0,0,1) = 예전 동작) + `쓰러지며 동시에 줄어들기`. 2D 카드는 판이 45도로 서 있어 월드 Z로 돌리면 원뿔을 그리며 화면에서 사선으로 넘어간다 → **카메라 정면 `(0, -0.7071, -0.7071)`**으로 화면 안에서만 도는 2D 회전이 된다. 회전은 오일러 덧셈이 아니라 **쿼터니언 앞곱셈**(월드 축 기준).
- **`CoinStack` 레인별 회전**: `회전 덮어쓰기`(기본 꺼짐) + `레인 회전 (Euler)`. 자원마다 메쉬가 세워진 축이 달라 전체 회전 하나로는 안 맞는다.
- **막힘 박스 플레이 중 표시**: `VirtualWall.플레이 중에도 표시` + `FenceBuilder.막는 박스를 플레이 중에도 표시`(둘 다 기본 꺼짐). **빌드에서는 켜져 있어도 안 나온다.** 스위치가 **두 군데**라 VirtualWall 쪽만 끄면 울타리 박스는 안 꺼진다.

---

## 4. 내가 낸 사고 2건 — 재발 방지

### (1) 툴팁 줄바꿈이 스크립트 번들을 통째로 죽였다

`@property`의 `tooltip`에 개행이 실제로 들어가 홑따옴표 문자열이 끊겼다. 증상은 그 파일 하나가 아니라 **번들 전체**다:

```
[Scene] Script "4e130q1bx9GyaEKCJDK6/rq" attached to "T_Tree_Fixed" is missing or invalid
[Scene] Script "6da1fN8TAxBWbxuFZlZUtVn" attached to "T_Worker" is missing or invalid
```

무관한 컴포넌트가 동시에 `MissingScript`가 되므로 **에러만 봐서는 원인 파일을 모른다.**

**규칙**: `@property`의 `tooltip`/`displayName`은 **한 줄**로. 편집 후 따옴표 짝 검사(각 줄의 홑따옴표 개수가 홀수인 `@property` 줄 찾기)를 돌린다. **번들이 죽은 동안 씬을 저장하지 않는다.**

### (2) `onLoad`에서 `node.active = false` → 머티리얼이 null이 됐다

`CoinGroundStack.onLoad`에서 `node.active = false`를 했다. `onLoad`는 엔진이 씬 트리를 **활성화하는 도중**에 불리므로, 그때 트리를 끄면 활성화 절차가 어긋나 초기화가 끝나지 않은 `MeshRenderer`가 남는다. 그 렌더러는 `_materials`가 null인 채로 터진다.

```
Cannot read properties of null (reading 'length')
  at MeshRenderer._isBatchingEnabled -> _updateModelParams -> _updateModels -> onLoad
  at NodeActivator.activateNode -> Scene._activate -> Director.runSceneImmediate
```

증상: **`ground_main`이 화면에서 사라짐** + 대장간 건설 시 에러.

**2차 피해**: 그 상태로 씬을 저장해 `_materials: null`이 **씬 파일에 직렬화**됐다. 에셋은 멀쩡했다(`T_GroundDirt.mtl`은 디스크와 library에 그대로).

**복구**: 씬 JSON에서 `__type__ == "CCPropertyOverrideInfo"` 중 `value`가 null인 것을 찾고, `git show HEAD:assets/scenes/tycoon.scene`의 같은 `localID` 오버라이드에서 값을 가져왔다(`localID ['271FbYuOtVCJONri+eejF9']` → `T_GroundDirt.mtl`).

**규칙**: `onLoad`에서 `node.active`를 끄지 않는다. 숨기려면 렌더러만 끄거나 `start()`에서 한다.

---

## 5. 마감 시점 상태 (파일 직접 검사)

| 항목 | 값 |
|---|---|
| `tycoon.scene` 오브젝트 / 노드 | 540 / 155 |
| `cc.MissingScript` | **0** |
| `value: null` 프로퍼티 오버라이드 | **0** |
| 스크립트 툴팁 따옴표 깨짐 | **0** |
| 디버그 `console.log` 잔재 | **0** |
| 재시작 후 부팅 로그 스크립트 에러 | **0** |

---

## 6. 다음에 할 일

### 화면으로만 판단 가능 (재시작 직후라 아직 못 봄)

1. `ground_main`(흙바닥)이 정상으로 보이는가
2. 대장간을 지어도 `null (reading 'length')` 에러가 안 나는가
3. 적재 소켓 7개가 건물 전까지 숨고, 지으면 커지며 나타나는가
4. 창고 짓기 전 일꾼이 짐을 진 채 서 있는가
5. 고용소 위에 `n/10`이 뜨는가
6. 나무가 좌/우로 눕는가 (사선 아님)
7. `ScatterExclude` 회전이 먹어 흙바닥 영역이 마름모와 맞는가

### 판단 대기

- **건물 5곳의 `ScatterExclude` 컴포넌트 체크박스를 켤 것인가**
- `Warehouse`의 옛 코인 무더기 중복(받는 자원=코인)을 지울 것인가
- 나무를 `씬에 굳히기`로 손 배치로 전환할 것인가

### 에셋 · 미구현

- 레일 비주얼(제작 예정), 레일 앵커·`SocketSpots` 마커 위치 눈으로 맞추기
- 일꾼 카운터 UI의 배경·아이콘
- 게임 종료 연출/CTA (`TriggerRelay`로 `GameClear`(103)까지는 이어져 있다)
- 소켓 요구 개수·게이지 시간·손님 간격·대장간 제작 시간 등 수치 튜닝

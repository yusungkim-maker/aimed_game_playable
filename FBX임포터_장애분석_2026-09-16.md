# FBX 임포터 등록 실패 장애 — 원인 분석 및 해결 방안

**작성일** 2026-09-16
**대상 프로젝트** `C:\Users\김유성\Desktop\cocos creator practice\np_playable_set`
**에디터** Cocos Creator 3.8.8 (설치일 2026-08-26, 설치본 코드 파일 무변경)
**상태** ✅ **캐릭터 복구 완료 (2026-09-16)** — 방안 B(glb 전환)로 해결. 정리 2건 남음. 아래 10장 참조

> 이 문서는 **확인된 것 / 추론한 것 / 모르는 것**을 구분해 표기한다.
> 모든 "확인"은 실제 파일·로그 실측에 근거하며, 근거 명령은 부록 B에 있다.

---

## 0. 한 줄 요약

**2026-09-16 10:19 에디터 부팅에서 `fbx` 에셋 핸들러 단 하나만 등록 목록에서 누락됐고**, 그 결과 asset-db가 프로젝트의 `.fbx` 59개와 **에디터 내장 `primitives.fbx`까지** 폴백 임포터(`*`)로 재임포트하면서 메시·스켈레톤·애니메이션 클립 서브에셋이 전부 사라졌다. 씬 1·2·3의 캐릭터가 동시에 안 보이는 이유가 이것이다.

**핸들러가 왜 누락됐는지는 아직 모른다.** 그 한 칸이 이 장애의 유일한 미해결 지점이다.

---

## 1. 증상

| 관찰 | 실제 |
|---|---|
| "씬 3에서 캐릭터가 안 보인다" | 씬 **1·2·3 전부** 안 보인다 |
| "`Player` 노드에 missing node가 있다" | **노드는 멀쩡하다.** 본 트리(`Bip001`~`Bone_Stick`)까지 전부 살아 있고, 끊긴 것은 **에셋 참조**뿐이다 |
| "나무·땅·2D 카드는 멀쩡하다" | 그것들은 `.glb`다. **`.fbx`만 전멸했다** — 이 비대칭이 진단의 지문이다 |

**확인:** 씬·프리팹 파일은 **무손상**이다. 세 씬 모두 원래 서브에셋 uuid를 그대로 들고 있다
(`tycoon.scene` 7건 / `scene.scene` 7건 / `xp_np.scene` 7건).
→ **임포터만 살리면 씬 손질 없이 자동 복구된다.**

---

## 2. 원인 사슬 — 확정

### 2.1 결정적 증거: 정상 부팅과 사고 부팅의 차이

`temp/asset-db/log/`에 **정상 동작하던 9/15 로그가 남아 있어** 직접 대조가 가능했다.
(이전 세션에서 "어제 로그가 없어 비교 불가"라고 기록했는데, **그 판단은 틀렸다.**)

부팅 시 asset-db가 등록하는 에셋 핸들러 목록을 뽑아 비교한 결과:

```
9/15 15:25 부팅 (정상)   … erp-texture-cube  fbx  gltf  gltf-animation …   ← 54개
9/16 10:19 부팅 (사고)   … erp-texture-cube       gltf  gltf-animation …   ← 53개
9/16 10:39 부팅 (재시작) … erp-texture-cube       gltf  gltf-animation …   ← 53개
```

**차이는 정확히 한 개. `fbx`만 빠졌다.** 나머지 53개(`gltf`, `gltf-mesh`, `gltf-skeleton`, `gltf-animation`, `material`, `image` …)는 세 부팅 모두 동일하다.

로그 건수로도 동일한 결론:

| 로그 | `lazy register … fbx` | `register asset-handler fbx failed` | `Can not find the importer fbx` |
|---|---|---|---|
| 9/15 10-36 | — (fbx 접근 없음) | **0** | 0 |
| 9/15 15-25 | **있음** | **0** | 0 |
| 9/16 10-19 | **없음** | **361** | **60** |
| 9/16 10-39 | **없음** | **960** | 0 |

### 2.2 누락 이후 일어난 일 (스택으로 확정)

```
ParallelQueue._generate                            ← .fbx 에셋마다 임포터를 찾는다
 → ImporterManager.find
 → AssetHandlerManager.findImporter
 → AssetHandlerManager._findImporterInRegisterInfo ← package.json 선언은 있으므로 찾긴 찾는다
 → AssetHandlerManager.activateRegister
 → new CustomImporter(t)
     ✖ TypeError: Cannot destructure property 'migrations' of 't.importer' as it is undefined
 → "register asset-handler fbx failed!"
 → "Can not find the importer fbx in editor"
 → 폴백 임포터 '*' 로 처리
```

**즉 선언(declaration)은 살아 있는데 구현(importer)이 비어 있는 상태다.**
`engine-extends/package.json`의 선언을 asar에서 직접 추출해 확인했다:

```json
{ "handler": "registerGltfFbxHandler", "name": "fbx", "extnames": [".fbx"] }
```

선언은 조건 분기 없이 평평한 배열 안에 있다. 즉 **선언은 언제나 존재**하며, 실패한 것은 `registerGltfFbxHandler`가 실제 importer를 공급하는 단계다.

### 2.3 폴백 임포터가 한 짓

`.fbx.meta`가 전부 아래로 덮였다:

```json
{ "ver": "1.0.0", "importer": "*", "files": [".fbx", ".json"], "subMetas": {} }
```

`subMetas: {}` → **메시·스켈레톤·애니메이션 클립 서브에셋이 통째로 소멸.**
`library/<uuid>/` 폴더도 비워졌다(잔존 서브에셋 0건).

**다행인 점:** `userData`는 살아남았다 — 클립 분할 이름, `wrapMode`, `previousId`, `assetFinder`가 전부 보존됐다. 이것이 복구의 근거 자료다.

### 2.4 왜 `.glb`는 멀쩡한가

Cocos에서 `.fbx`는 1급 포맷이 아니다. 임포트 시점에 **외부 변환기로 glTF로 바꾼 뒤** glTF 파이프라인을 태운다.

```
.glb  →                                     [gltf 핸들러] → 메시·스켈레톤·클립
.fbx  →  [fbx 핸들러] → FBX-glTF-conv.exe → [gltf 핸들러] → 메시·스켈레톤·클립
            ↑ 이 칸만 비었다
```

`.glb`에는 이 앞단이 **아예 없다.** 그래서 `.fbx` 59/59 전멸, `.glb` 0건 피해라는 비대칭이 나온다.
그리고 **플레이어 캐릭터만 `.fbx`였기 때문에 캐릭터만 사라졌다.**

---

## 3. 근본 원인 — 확정된 것과 아닌 것

### ✅ 확정

1. `fbx` 핸들러가 9/16 부팅부터 등록 목록에서 누락됐다. (로그 실측, 3회 부팅 대조)
2. 누락 지점은 **`registerGltfFbxHandler`가 importer를 공급하는 단계**다. (스택 + package.json 선언 대조)
3. 이 누락이 폴백 임포터 `*`를 불러 meta 59+1개를 덮었다. (meta 내용 + 타임스탬프 일치)
4. 피해 시각은 **2026-09-16 10:19:23~24**. 에디터 내장 `primitives.fbx.meta`의 mtime이 10:19:23으로 정확히 일치한다.

### ❓ 미확정 — 이것이 유일한 남은 질문

> **`registerGltfFbxHandler`가 왜 importer를 공급하지 못했는가?**

에디터의 해당 코드(`dist/handler/assets/fbx.ccc`, `dist/importer/importers/fbx.ccc`)는 **암호화된 `.ccc`**라 소스로 확인이 불가능하다. asar에서 추출해 확인한 결과 암호문이었다.

### ⛔ 배제된 것 (근거 있음)

| 가설 | 배제 근거 |
|---|---|
| FBX 파일이 깨졌다 | 59개 전부 유효한 바이너리 FBX (`Kaydara FBX Binary`, ver 7400×56 / 7500×3). 사용자도 직접 확인 |
| 최근 임포트한 캐릭터가 원인 | `.fbx` 중 8/31 이후 들어온 파일 **0개**. 9/15~16 신규 모델은 전부 `.glb` |
| Bob_new 임포트가 임포터를 깼다 | **9/15 16:01:49에 Bob_new fbx를 임포트했고 성공했다** (로그에 `Import: …Bob_new\NPC_TTSummon_001_Mesh.fbx@17318` 존재). 그 후 3시간 넘게 정상 동작 |
| uuid 중복 | 프로젝트 전체 meta uuid 중복 **0건** |
| 변환기 바이너리 손상/누락 | `FBX-glTF-conv.exe` (8.7MB) 존재, 실행 확인 (`v1.0.0-alpha.49.editor.1`) |
| 변환기 경로 해석 실패 | `@cocos/fbx-gltf-conv/index.js`(비암호화) 확인 — `__dirname` 기반 단순 경로, 파일 존재 |
| 에디터 설치본 코드 손상 | 설치일(8/26) 이후 변경된 파일은 **위 meta 1개 + import-map 캐시 2개뿐** |
| 프로젝트 확장이 방해 | `cocos-mcp-server` / `playable-ads-adapter` 둘 다 `asset-db` 기여 없음. 9/15에도 동일하게 로드됨 |
| 전역 확장 / builtin 오버라이드 | `~/.CocosCreator/extensions`는 `playable-ads-adapter` 하나, `builtin-extensions/3.8.8`은 **빈 폴더** |
| Feature Cropping | `~/.CocosCreator/profiles/features.json`은 미니게임 플랫폼 플래그뿐, fbx 무관 |
| `library` 삭제 흔적 | 없음 |
| `temp/`에 핸들러 레지스트리 캐시 | 그런 파일 없음 — 레지스트리는 매 부팅 package.json에서 구성됨 |

### 🔍 남은 가설과 각각의 검증법

| # | 가설 | 근거 강도 | 검증 방법 |
|---|---|---|---|
| 1 | **`registerGltfFbxHandler`가 런타임 검사(예: 변환기 가용성)에 실패해 스스로 등록을 포기** | 중 — 9/16 로그에 `FBX-glTF-conv` 흔적이 **0건**(9/15엔 42건). 변환기를 아예 안 건드렸다는 뜻 | 에디터를 **관리자 권한**으로 실행 / 백신 실시간 검사에서 에디터·변환기 경로 예외 처리 후 재시작 |
| 2 | `temp/` 캐시가 초기화 순서를 틀어놓음 | 하 — 해당 캐시 파일을 못 찾음. 다만 비용이 0이라 먼저 해볼 값어치 있음 | `temp/` 삭제 후 재시작 |
| 3 | 9/15 19:17 **빌드 도중 세션이 끊긴 것**이 남긴 상태 | 하~중 — 9/15 로그가 빌드 진행 **15% 지점에서 끊겨 있다**(정상 종료 흔적 없음). 다만 영향 경로 미상 | `temp/` + `library/` 삭제 후 재시작 |
| 4 | 설치본 무결성 문제(파일 mtime엔 안 잡히는 종류) | 하 — 코드 파일 무변경 확인됨 | 에디터 재설치 |

**추측으로 메우지 말 것.** 위 네 가지는 전부 가설이며, 1번이 가장 근거가 있지만 확정은 아니다.

---

## 4. 피해 범위

### 4.1 프로젝트 내부

- `assets/**/*.fbx.meta` **59/59 손상** (`importer:"*"`, `subMetas:{}`)
- `library/` 내 해당 서브에셋 **전부 삭제됨** (uuid 폴더가 빈 채로 남음)
- 끊긴 참조 **54건 / 10개 파일**

```
scene.scene       7      xp_np.scene       7      tycoon.scene      7
T_Player.prefab   7      T_Worker.prefab   7
Follower.prefab   5      Worker.prefab     5
PlayerAnimGraph   3      WorkerAnimGraph   3      T_CharAnimGraph   3
```

### 4.2 🔴 프로젝트 **밖** — 에디터 설치본

```
C:\ProgramData\cocos\editors\Creator\3.8.8\...\engine\editor\assets\primitives.fbx.meta
  mtime 2026-09-16 10:19:23   importer:"*"   subMetas:{}
```

엔진 내장 프리미티브(**Box / Sphere / Plane / Quad / Capsule / Cylinder / Cone / Torus**)가 같이 깨졌다.
내장 DB가 프로젝트 `assets/`보다 먼저 마운트되므로 **이것이 첫 번째 희생자**다(원인이 아니라 증상).

> ⚠️ **이 파일은 프로젝트 `temp/`·`library/`를 지워도 복구되지 않는다.**
> 이 PC의 **모든 Cocos 프로젝트**에 영향을 준다. 에디터 재설치의 근거가 여기 있다.
> (단, fbx 핸들러가 되살아나면 다음 부팅에 자동 재임포트될 가능성이 있다 — **미확정**)

### 4.3 백업 (복구 자산)

`C:\Users\김유성\Desktop\fbx_meta_backup_2026-09-16\` — **59개 온전**, `previousId` 55개 보유.
클립 이름 55개와 `wrapMode` 값이 전부 남아 있다. **재임포트가 또 실패해 meta가 다시 덮이면 이것이 유일한 복원 소스다.**

---

## 5. 해결 방안

### 방안 A — 임포터 복구 (원상복귀)

목표: `fbx` 핸들러를 되살려 uuid·서브에셋 id가 그대로 돌아오게 한다. 성공하면 **54건 리맵이 아예 불필요**하다.

**A-1. `temp/` 삭제 후 재시작** — 비용 1분, 위험 0 (순수 캐시)

**A-2. 백신/권한** — 에디터를 관리자 권한으로 실행, 또는 백신 실시간 검사에서 아래 경로 예외 처리

```
C:\ProgramData\cocos\editors\Creator\3.8.8\
  …\app.asar.unpacked\node_modules\@cocos\fbx-gltf-conv\bin\win32\FBX-glTF-conv.exe
```

(가설 1이 맞다면 여기서 풀린다. 9/16 로그에 변환기 흔적이 0건인 것이 유일한 직접 근거다.)

**A-3. `library/` 삭제 후 전체 재임포트** — 시간 소요, 위험 낮음

**A-4. 에디터 재설치** — 내장 `primitives.fbx.meta`를 원본으로 되돌리는 **유일한 확실한 방법**

> **A 성공 판정 기준**
> 1. `temp/logs/project.log`에 `register asset-handler fbx failed` **0건**
> 2. `assets/**/*.fbx.meta`의 `importer:"*"` 개수 **0**, `subMetas` 채워짐
> 3. **서브에셋 id 대조** — 캐릭터 메시 `add93f0a-…`의 `@30732 @0809f @2224c @c1fef @e66be @f1394`,
>    공격 클립 `dfde3a72-…@36e9f`. 일치하면 자동 복구 완료. 어긋나면 uuid 치환으로 넘어간다.

**장점** uuid 유지, 씬·프리팹 무수정
**단점** 원인 미상이라 **성공을 보장할 수 없다.** A-4까지 가도 재발 가능성이 남는다

---

### 방안 B — `.fbx` → `.glb` 전면 전환 (의존 제거)

목표: fbx 경로 자체를 프로젝트에서 없앤다. 이 장애가 재발해도 캐릭터가 죽지 않는다.

#### ✅ 실현 가능성 검증 완료 (2026-09-16, 프로젝트 파일 무변경)

에디터가 쓰는 **바로 그 변환기**를 직접 실행해 전 과정을 검증했다.

**메시 변환 결과가 원본 임포트 결과와 정확히 일치:**

| 항목 | 백업 meta (원본) | 변환 결과 |
|---|---|---|
| 메시 | 6 | **6** — `NPC_Summon_001` `Prop_Axe01` `Wagon` `Hammer` `crossbow001` `crossbow002` |
| 스켈레톤 | 6 | **6** |
| 머티리얼 | 2 | **2** |
| 본 | — | `Bip001` `Bip001 Pelvis` `Bone_Stick` `Bone_CrossBow` `Bone_Wagon` `Bone_Hammer` `Root` **전부 존재** |

본 이름이 씬에 직렬화된 노드 트리와 **완전히 동일**하다. ← **프리팹을 다시 만들지 않아도 되는 근거**

**클립 이름 문제도 해결 확인:**
변환하면 애니메이션 이름이 원본 take 이름 `Take 001`로 나온다(현재 `Axe_Idle`은 FBX 임포터에서 자른 **사용자 지정 이름**). CLAUDE.md의 *"애니메이션 클립 이름이 인터페이스다"*에 해당하므로 어긋나면 조용히 깨진다.
→ glTF JSON의 `animations[0].name`을 패치한 뒤 glb로 다시 묶어 검증했다:

```
axe_idle.glb  75,188 bytes   magic=glTF  ver=2   animations=['Axe_Idle']   ✓
```

**백업 meta에 55개 클립의 이름과 `wrapMode`가 전부 남아 있어 전 과정 자동화가 가능하다.**

#### 작업 내용

1. 59개 `.fbx` → `.gltf` 변환 (Cocos 번들 `FBX-glTF-conv.exe`)
2. `animations[].name`을 백업 meta의 split 이름으로 패치
3. `.glb`로 패킹 (헤더 + JSON 청크 + BIN 청크)
4. 에디터로 임포트 → 새 uuid / 서브에셋 id 수집
5. **10개 파일 54건 uuid 치환** (재제작 아님, 텍스트 리맵)
6. 클립 `wrapMode` 재설정 (백업 값 그대로)

**장점** 확실함(glb 경로는 지금도 정상 동작 중) · 포맷 통일 · **재사용 모듈 라이브러리라는 프로젝트 목적과 방향이 맞음**
**단점** uuid 전부 변경 → 54건 리맵 필요 · 변환 옵션이 에디터 것과 100% 같다는 보장은 없음(스케일·축, 1개 시험으로 검증 가능)
**한계** **원인 제거가 아니라 의존 제거다.** 내장 `primitives.fbx`는 그대로 깨져 있고, 앞으로 새 `.fbx`를 넣으면 같은 일이 난다

---

### 방안 비교

| | A. 임포터 복구 | B. glb 전환 |
|---|---|---|
| uuid | **유지** | 전부 변경 → 54건 리맵 |
| 씬·프리팹 | 무수정 | uuid 치환만 (재제작 불필요) |
| 작업량 | 거의 없음 | 중 (자동화 가능) |
| 성공 확실성 | **낮음** (원인 미상) | **높음** (검증 완료) |
| 재발 방지 | 없음 | **프로젝트 한정으로 있음** |
| 내장 primitives 복구 | A-4(재설치)만 가능 | 불가 |

---

## 6. 권장 절차

```
1. A-1  temp/ 삭제 → 재시작 → 판정 기준 3개 확인          [1분, 위험 0]
        └ 성공 → 종료. 씬 손질 불필요.
2. A-2  백신 예외 + 관리자 권한 실행 → 재시작              [5분]   ← 가설 1 검증
        └ 성공 → 종료 + 원인 확정(기록 가치 큼)
3. A-3  library/ 삭제 → 전체 재임포트                      [10~30분]
4. A-4  에디터 재설치                                      [30분]  ← 내장 primitives도 복구
5. 여기까지 실패 → B 실행                                  [반나절]
```

**B는 A와 무관하게 언젠가 할 값어치가 있다.** A가 성공해도, 캐릭터를 glb로 옮겨두면 이 장애 계열에 다시 걸리지 않는다. 다만 **A를 먼저 시도하는 것이 압도적으로 싸다** — 성공하면 리맵 54건이 통째로 불필요해진다.

🔴 **어느 경로든 복구 확인 전까지 빌드 금지.**

---

## 7. 부록 A — 핵심 증거 원문

**A-1. 핸들러 등록 목록 차이 (가장 결정적)**

```
9/15 15-25 로그:  … erp-texture-cube  fbx  gltf  gltf-animation …
9/16 10-19 로그:  … erp-texture-cube       gltf  gltf-animation …
9/16 10-39 로그:  … erp-texture-cube       gltf  gltf-animation …
차이 = fbx 1개. 나머지 53개 동일.
```

**A-2. 실패 스택**

```
TypeError: Cannot destructure property 'migrations' of 't.importer' as it is undefined.
    at new CustomImporter (…/builtin/asset-db/dist/worker/manager/asset-handler-manager.ccc:1:815)
    at AssetHandlerManager.activateRegister (…:1:3868)
    at AssetHandlerManager._findImporterInRegisterInfo (…:1:5429)
    at AssetHandlerManager.findImporter (…:1:5087)
    at ImporterManager.find (…/asset-db-manager.ccc:1:8402)
    at ParallelQueue._generate (…/@editor/asset-db/libs/asset-db.ccc:145:30)
```

**A-3. 핸들러 선언 (asar에서 추출)**

```json
{ "handler": "registerGltfFbxHandler", "name": "fbx", "extnames": [".fbx"] }
```

출처: `/modules/engine-extensions/extensions/engine-extends/package.json` → `contributions["asset-db"]["asset-handler"]`

**A-4. 변환기 접근 흔적 (가설 1의 근거)**

```
9/15 15-25 로그:  FBX-glTF-conv\<uuid>\status.json 참조 42건
9/16 10-19 로그:  0건
```

**A-5. 손상된 meta 형태**

```json
{ "ver": "1.0.0", "importer": "*", "imported": true,
  "uuid": "add93f0a-c01f-47b2-b7f7-1a9f0a4ec81a",
  "files": [".fbx", ".json"], "subMetas": {},
  "userData": { "…살아남음…": true } }
```

**A-6. 타임라인**

```
09-15 15:25   부팅. fbx 핸들러 등록 O. 정상
09-15 16:01   Bob_new fbx 임포트 성공 (변환기 정상 동작)
09-15 16:46   T_Body.mtl 전환, 캐릭터 정상 확인
09-15 19:17   로그가 빌드 진행 15% 지점에서 끊김 (정상 종료 흔적 없음)
09-16 10:19   부팅. fbx 핸들러 누락 → 사고 발생 (10:19:23~24)
09-16 10:39   재시작. 동일 실패 (재시작으로 안 풀림)
```

---

## 8. 부록 B — 검증 명령어

```bash
# 1) 핸들러 등록 목록 비교 (가장 중요)
cd "<프로젝트>/temp/asset-db/log"
grep -a -o "lazy register asset handler [a-z-]*" "<로그파일>" | sort -u

# 2) fbx 실패 건수
grep -a -c "register asset-handler fbx failed" temp/logs/project.log

# 3) 깨진 meta 개수 (0 이어야 정상)
grep -l '"importer": "\*"' $(find assets -name "*.fbx.meta") | wc -l

# 4) 씬이 참조하는 서브에셋 id 생존 확인
grep -o "add93f0a-c01f-47b2-b7f7-1a9f0a4ec81a@[0-9a-f]*" assets/tycoon.scene | sort -u

# 5) FBX 파일 무결성  ("Kaydara FBX Binary  " 이어야 함)
find assets -name "*.fbx" | while read f; do head -c 20 "$f" | tr -d '\0'; echo "  $f"; done

# 6) 에디터 내장 meta 상태
cat "C:/ProgramData/cocos/editors/Creator/3.8.8/resources/resources/3d/engine/editor/assets/primitives.fbx.meta"

# 7) 변환기 동작 확인
"C:/ProgramData/cocos/editors/Creator/3.8.8/resources/app.asar.unpacked/node_modules/@cocos/fbx-gltf-conv/bin/win32/FBX-glTF-conv.exe" --version
```

---

## 9. 부록 C — 이 장애와 무관하지만 발견된 것

1. **`assets/door1undefined`** — 확장자 없이 glb 바이너리가 든 파일. gltf 임포터가 부팅마다 `JSON.parse`에 실패한다. fbx 실패보다 2초 먼저 발생하지만 **인과관계 없음**(gltf 핸들러는 정상 등록되고 다른 glb는 전부 임포트됨). ⚠️ **삭제 금지** — 씬 1·2가 이 파일의 메시를 참조한다.
2. **`assets/texture/ground_fogundefined`** — 같은 형태의 PNG. `image` 임포터가 정상 처리 중이므로 문제 없음.
3. **CLAUDE.md 정정 필요** — 씬 3 `T_Body.mtl`은 `builtin-standard`가 아니라 **`legacy/standard`**(`1baf0fc9-…`)다. `builtin-standard`(`c8f66d17-…`)를 쓰는 것은 `T_Axe.mtl` 쪽이다.
4. **규칙 9 예외 발견** — 씬 3의 `clipAttack`이 `assets/NPC_Summon/Animation/@Axe_Attack01.fbx`를 참조한다. `assets/tycoon/` **밖의 공유 자산**이므로 "씬 3이 참조하는 공유 프로젝트 자산 0건"은 사실이 아니다.
5. **씬 1·2 캐릭터는 툰 렌더** — `bob01.mtl` = `legacy/toon` (`USE_OUTLINE_PASS` + 2단계 셰이드). Player·Follower·Worker가 공유한다. 씬 3만 PBR로 갈아탄 상태다.
6. **같은 캐릭터 메시 fbx가 4벌 존재** — md5는 전부 동일(`9e023566…`)하지만 uuid는 4개 전부 다르다. 씬 1·2는 `9e93cbfa…`, 씬 3은 `add93f0a…`를 쓰고, `NPC_Summon/` 루트 사본과 `Bob_new/` 사본은 **미사용**이다. B 실행 시 변환 대상을 줄일 여지가 있다.


---

## 10. 결과 — 방안 B로 해결 (2026-09-16 당일)

**방안 A(임포터 복구)는 시도하지 않고 방안 B(glb 전환)로 갔다.** 사용자가 Blender에서 뽑은 `bob.glb`가 정상 렌더되는 것을 확인해 glb 경로의 건강함이 입증됐고, 그 시점에 원인 규명보다 의존 제거가 확실했기 때문이다.

### 수행 내역

| 단계 | 결과 |
|---|---|
| `.fbx` 59개 → `.glb` 변환 | **59/59 성공**, 경고·실패 0. 5.3MB (원본 19.2MB) |
| 애니메이션 이름 패치 | 55개 클립 전부 프로젝트가 쓰던 split 이름으로 복원 (`Take 001` 잔존 0) |
| 프로젝트 설치 | **51개** — 실사용 10 + 모션 라이브러리 41 (`Bob_new/animation/`) |
| uuid 리맵 | **54건 / 10개 파일**, 구 uuid 잔존 0건 |
| 참조 검증 | **101건 전부 실재 서브에셋으로 해결** (미해결 0) |
| 눈 확인 | 씬 1·2·3 캐릭터 정상 |

### 예상 밖의 수확 — 수동 작업이 0이 됐다

클립 8개(실사용분)의 **이름·`wrapMode`·fps·구간이 원본과 완전히 일치**했고, **서브에셋 id까지 동일**했다:

```
Idle c2696/c2696   Move 2bc6c/2bc6c   One_BT_Attack01 199a1/199a1
One_BT_Idle ed136/ed136   One_BT_Move 68ed9/68ed9
Axe_Attack01 36e9f/36e9f   Axe_Idle 6982a/6982a   Axe_Move 6d6d9/6d6d9
```

서브에셋 id가 **클립 이름에서 결정되는 값**이라 그대로 재생성된 것이다. 바뀐 것은 부모 uuid뿐이라 리맵이 단순 치환으로 끝났고, 5장에서 예상했던 "클립 55개 `wrapMode` 재설정"이 통째로 불필요해졌다.

또한 **프리팹을 다시 만들 필요가 없었다.** 본 계층이 씬·프리팹에 노드로 직렬화되어 있고 변환본의 본 이름·계층이 원본과 동일했기 때문에, `__uuid__` 문자열만 갈아끼우면 됐다.

### 부수 성과 — 규칙 9 위반 시정

씬 3이 쓰던 `@Axe_Attack01` `@Axe_Idle` `@Axe_Move` 3개가 **공유 폴더 `NPC_Summon/Animation/`을 참조**하고 있었다(부록 C-4에서 발견). uuid를 어차피 전부 바꾸는 김에 `tycoon/models/`로 분기해 고쳤다.
반대로 `tycoon/models/`에 분기해둔 `@Idle` `@Move` `@One_BT_Attack01` 3개는 **아무도 쓰지 않는 오배치**였다(씬 1·2용 클립을 잘못 복사해둔 것). 버렸다.

> 교훈: **"tycoon/ 아래에 사본이 있다"는 격리의 증거가 아니다.** 실제 참조를 전수 검색해야 한다.

### 🔲 남은 일 2가지

1. **구 `.fbx` 59개 + `.bak` 10개 삭제** — 반드시 에디터 Assets 패널 또는 MCP `asset_operations delete`로(규칙 7). 검색창 `fbx` → 전체 선택 → Delete, 이어서 `bak`. 지우면 부팅 시 `register asset-handler fbx failed`도 사라진다. 그 전까지 이 에러는 계속 나오지만 **캐릭터와 무관하다.**
2. **에디터 내장 `primitives.fbx`는 여전히 깨진 상태** — 기본 도형(Box/Sphere/Plane/Quad 등)을 쓸 일이 생기면 **에디터 재설치**가 필요하다. 프로젝트 `temp/`·`library/` 삭제로는 복구되지 않는다.

### 남은 미확정

**`registerGltfFbxHandler`가 importer를 공급하지 못한 이유는 끝내 밝히지 못했다.** 3장의 가설 4개는 그대로 열려 있다. 다만 프로젝트가 더 이상 `.fbx`에 의존하지 않으므로 이 장애 계열에 다시 걸리지는 않는다 — **원인 제거가 아니라 의존 제거임을 기억할 것.**

### 보관함

`./fbx_to_glb_2026-09-16/` — 변환본 59개 / 리맵 전 원본 10개 / 재현 스크립트 4개 + 대응표. 자세한 내용은 그 폴더의 `README.md`.
